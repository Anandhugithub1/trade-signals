pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
yfinance>=0.2.40
tabulate>=0.9.0
# Optional — only needed to write signals to Supabase / apply the schema.
# The code degrades gracefully if these are absent.
supabase>=2.4.0
psycopg[binary]>=3.1.0
